/** Prompt row surface — keep slash overlays and modal scrims in sync to avoid seams. */
export const PROMPT_INPUT_SURFACE_BG = '#3E3E52'
/** Left accent strip on the prompt (must stay clear of fullscreen slash overlay paint). */
export const PROMPT_INPUT_LEFT_ACCENT_BG = '#C8B6FF'
